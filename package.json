{
  "name": "cucumber-cypress-casino",
  "version": "1.0.0",
  "description": "1. a directory for the project\r 2. Open that directory into the VS Code\r 3. Execute the command for npm init -y",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:run": "cypress run",
    "cypress:open": "cypress open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^21.0.3",
    "cypress": "^13.15.2"
  }
}
